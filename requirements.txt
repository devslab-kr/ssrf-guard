# Documentation site build dependencies.
# Pinned to known-working versions so docs builds are reproducible.
# Local preview: pip install -r docs/requirements.txt && mkdocs serve
mkdocs-material>=9.5,<10
mkdocs-static-i18n[material]>=1.2,<2
pymdown-extensions>=10.0,<11

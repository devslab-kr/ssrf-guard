# Tests run with SSRF Guard enabled by default — same as production. Individual
# tests that need to exercise the disabled path override via @SpringBootTest
# properties or @DynamicPropertySource.
ssrf.guard.enabled=true
